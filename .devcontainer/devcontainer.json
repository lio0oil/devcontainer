// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/python
{
  "name": "devcontainer",
  "dockerComposeFile": ["docker-compose.yml"],
  "service": "devcontainer",
  "workspaceFolder": "/home/vscode/workspace",

  // Use 'postCreateCommand' to run commands after the container is created.
  "postCreateCommand": "/bin/sh /home/vscode/workspace/.devcontainer/postCreateCommand.sh",

  // Install VS Code extensions on attach
  "postAttachCommand": "/bin/sh /home/vscode/workspace/.devcontainer/.dotconfig/.scripts/vscode_extension.sh",

  // Configure tool-specific properties.
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.defaultProfile.linux": "zsh"
      }
    }
  },

  "remoteUser": "vscode"
}
